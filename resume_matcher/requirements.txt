pdfplumber>=0.11
pypdf>=5.0
scikit-learn>=1.4
numpy>=1.26
sentence-transformers>=2.2
torch>=2.0
pytest>=8.0

# Optional — only needed to (a) generate the bundled sample PDFs, and
# (b) enable LLM-written explanations instead of the template fallback.
reportlab>=4.0
anthropic>=0.30
